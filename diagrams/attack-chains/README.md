# Attack Chains

Attack-chain and kill-chain diagrams for the security labs (e.g. the AD
Kerberoasting → lateral-movement path). Export as images and reference them from
the relevant lab README, e.g. `ad-kerberoasting-chain.png`.
